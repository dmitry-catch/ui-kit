import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks'
import { iconNames } from './general/icon/iconNames.ts'

<Meta title="Icons" />

<Title>Icons</Title>

<IconGallery>
	{iconNames.map((name) => (
		<IconItem name={name}>
			<img src={`../../src/components/general/icon/icons/${name}-24px.svg?raw`} alt={name} />
		</IconItem>
	))}
</IconGallery>
